services:
  ministack:
    image: ministackorg/ministack
    container_name: ministack
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3,rds,apigateway,sts
      - DEBUG=0
      - AWS_ACCESS_KEY_ID=test
      - AWS_SECRET_ACCESS_KEY=test
      - AWS_DEFAULT_REGION=us-east-1
      - DOCKER_HOST=unix:///var/run/docker.sock
    volumes:
      - ministack_data:/var/lib/localstack
      - /var/run/docker.sock:/var/run/docker.sock
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:4566/_localstack/health')\""]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  infra-provisioner:
    image: hashicorp/terraform:latest
    container_name: infra-provisioner
    working_dir: /infra
    volumes:
      - ./terraform:/infra
    environment:
      - TF_VAR_endpoint=http://ministack:4566
      - TF_VAR_db_name=${DB_NAME:-plus_auth}
      - TF_VAR_db_user=${DB_USER:-plus}
      - TF_VAR_db_password=${DB_PASSWORD:-plus_secret}
      - TF_VAR_ms_auth_port=${MS_AUTH_PORT:-3001}
    entrypoint: ["/bin/sh", "-c", "rm -f terraform.tfstate terraform.tfstate.backup && terraform init && terraform apply -auto-approve"]
    depends_on:
      ministack:
        condition: service_healthy

  plus-ms-auth:
    build:
      context: ../plus-ms-auth
      dockerfile: Dockerfile
    container_name: plus-ms-auth
    ports:
      - "${MS_AUTH_PORT:-3001}:3001"
    environment:
      - PORT=3001
      - JWT_SECRET=${JWT_SECRET:-dev-secret}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
    depends_on:
      infra-provisioner:
        condition: service_completed_successfully
    restart: unless-stopped

  plus-ms-product:
    build:
      context: ../plus-ms-product
      dockerfile: Dockerfile
    container_name: plus-ms-product
    ports:
      - "${MS_PRODUCT_PORT:-3002}:3002"
    environment:
      - PORT=3002
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - JWT_SECRET=${JWT_SECRET:-dev-secret}
    depends_on:
      infra-provisioner:
        condition: service_completed_successfully
    restart: unless-stopped

  plus-mfe-auth:
    build:
      context: ../plus-mfe-auth
      dockerfile: Dockerfile
      args:
        - VITE_MS_AUTH_URL=http://localhost:${MS_AUTH_PORT:-3001}
    container_name: plus-mfe-auth
    ports:
      - "${MFE_AUTH_PORT:-4001}:4001"
    depends_on:
      infra-provisioner:
        condition: service_completed_successfully
    restart: unless-stopped

  plus-mfe-product:
    build:
      context: ../plus-mfe-product
      dockerfile: Dockerfile
      args:
        - VITE_PRODUCT_API_URL=http://localhost:${MS_PRODUCT_PORT:-3002}
    container_name: plus-mfe-product
    ports:
      - "${MFE_PRODUCT_PORT:-4002}:4002"
    depends_on:
      plus-ms-product:
        condition: service_started
    restart: unless-stopped

  plus-shell:
    build:
      context: ../plus-shell
      dockerfile: Dockerfile
      args:
        - MFE_AUTH_URL=http://localhost:${MFE_AUTH_PORT:-4001}/assets/remoteEntry.js
        - MFE_PRODUCT_URL=http://localhost:${MFE_PRODUCT_PORT:-4002}/assets/remoteEntry.js
    container_name: plus-shell
    ports:
      - "${SHELL_PORT:-3000}:3000"
    depends_on:
      plus-mfe-auth:
        condition: service_started
      plus-mfe-product:
        condition: service_started
    restart: unless-stopped

volumes:
  ministack_data:
